workflows:
  android-workflow:
    name: Android Workflow
    environment:
      java: 17
    scripts:
      - name: Build debug AAB
        script: |
          ./gradlew bundleDebug
    artifacts:
      - app/build/outputs/**/*.aab
